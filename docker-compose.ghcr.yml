services:
  fal-image-gen:
    image: ghcr.io/glh08/fal-image:latest
    container_name: fal-image-generator-pro
    ports:
      - "8787:8787"
    environment:
      - FAL_KEY=${FAL_KEY}
      - PORT=8787
      - LSKY_URL=${LSKY_URL:-}
      - LSKY_TOKEN=${LSKY_TOKEN:-}
      - LSKY_STRATEGY_ID=${LSKY_STRATEGY_ID:-1}
      - AUTH_PASSWORD=${AUTH_PASSWORD:-}
      - RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS:-100}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8787/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
